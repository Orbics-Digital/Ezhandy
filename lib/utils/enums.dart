// enum AuthRole { email, phone, google, apple }

enum BookingType { Pending, Completed, Assigned, InRoute }

enum CampaignType { Pending, Rejected, Accepted,New}

enum MediaPathType { file, network, asset }

enum MediaType { image, pdf, png, jpg }

enum OtpType { signup, forget }

enum OtpCodeType { phone, email }

enum ServiceType { instant, schedule }

// enum OptionType { message, call, email }

// enum SocialType { email, google,facebook, apple, phone }

enum WebContentType { tc, pp, ap, rp }

// enum ProfileType { create, edit, bottomBar, none }

enum AddEditType { add, edit }

// enum StatusType { Approved, Pending, Canceled, Past }

// enum OrderStatusType { NewOrder, Pending, Canceled }

// enum GraphType { Weekly, Monthly, Yearly }

// enum rideSelectionStatus { selectAddress, confirmLocation }

// enum rideRequestStatus { rideAccept, rideStart, rideComplete }
